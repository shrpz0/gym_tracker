services:
  django:
    build: ./backend
    container_name: django_app
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    env_file:
      - .env

  db:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: redis_server
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis_data:/data

  celery:
    build: ./backend
    command: celery -A gym_tracker_django worker --loglevel=info
    volumes:
      - ./backend:/app
    depends_on:
      - redis
      - django
    env_file:
      - .env

  beat:
    build: ./backend
    command: celery -A gym_tracker_django beat -l info
    volumes:
      - ./backend:/app
    depends_on:
      - redis
      - django
    env_file:
      - .env

volumes:
  postgres_data:
  redis_data:
